# Run tests
php artisan test

# Test API with curl
curl -X POST http://localhost/api/login \
    -H "Content-Type: application/json" \
    -d '{
        "phone": "03001234567",
        "password": "password123",
        "device_id": "test-device-001"
    }'

# Test transfer
curl -X POST http://localhost/api/transfer/p2p \
    -H "Authorization: Bearer TOKEN" \
    -H "Content-Type: application/json" \
    -d '{
        "receiver_phone": "03001112222",
        "amount": 100,
        "pin": "1234"
    }'
